// The entire delivery pipeline is provided by the shared library.
// This repository declares only its identity (deploy-config.yaml);
// no pipeline logic lives here, and none is duplicated per project.
@Library('finacplus-cicd@main') _

deliveryPipeline()
